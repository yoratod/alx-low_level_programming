#include <stdio.h>
/*
 * main - Always print the correct output
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int n = 2;

	if (n > 0)
	{
		printf("positive");
	}
	else
	{
		printf("negative");
	}
	return (0);
}
