#ifdef _HEADER_
#include <stdio.h>
#include "dog.h"
/**
 * main - check the code by defining new struct
 *
 * @dog :return d;
 * @name : include *name
 * Return: Always 0.
 */
int main(struct dog *d,char  *name,float age, char *owner)
{
	if(d != null)
	{
        d-> *name;
        d->  age;
        d->  *owner;
	}
};
#endif
