#include <stdio.h>

/**
 * main - Prints the alphabet in lowercase.
 *
 * Return: Always 0.
 */
int main(void)
{

_islower(();
        return (0);
}
int _islower(int c)
{
        
	for (c = 0; c < 10; c++)
            {
             putchar(c);
            }

        putchar('\n');
}

}
